name: Release Praxis

on:
  push:
    tags:
      - "praxis-v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup MSYS2 CLANG64
        uses: msys2/setup-msys2@v2
        with:
          msystem: CLANG64
          update: true
          pacboy: >-
            toolchain:p
            cmake:p
            ninja:p

      - name: Get version from tag
        id: version
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#praxis-v}"
          echo "VERSION=$VERSION" >> "$GITHUB_OUTPUT"
          if [[ "$VERSION" == *-* ]]; then
            echo "PRERELEASE=true" >> "$GITHUB_OUTPUT"
          else
            echo "PRERELEASE=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Extract changelog
        id: changelog
        shell: bash
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          {
            echo "BODY<<CHANGELOG_EOF"
            awk -v ver="$VERSION" '
              /^## \[/ { if (found) exit; if (index($0, "[" ver "]")) { found=1; next } }
              found { print }
            ' CHANGELOG-Praxis.md
            echo "CHANGELOG_EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Configure CMake
        shell: msys2 {0}
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=MinSizeRel -DRELEASE_USE_STATIC_CRT=ON -DRELEASE_USE_LTO=ON

      - name: Build
        shell: msys2 {0}
        run: cmake --build build --config MinSizeRel --target praxis

      - name: Package
        shell: bash
        run: |
          mkdir release
          cp build/bin/Praxis.exe release/
          cp src/Praxis/README.md release/README.md
          cp LICENSE release/
          cp CHANGELOG-Praxis.md release/CHANGELOG.md

      - name: Create archive
        shell: pwsh
        run: Compress-Archive -Path release/* -DestinationPath "Praxis-${{ steps.version.outputs.VERSION }}.zip"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Praxis v${{ steps.version.outputs.VERSION }}
          body: ${{ steps.changelog.outputs.BODY }}
          prerelease: ${{ steps.version.outputs.PRERELEASE }}
          files: Praxis-${{ steps.version.outputs.VERSION }}.zip
